function addEnds(arr) {
  let sum = 0;
  for (let i = 0; i < arr.length; i++) {
    sum = list[0] + list[4];
  }
  return sum;
}
let list = [17, 8, 9, 5, 20];
let sum = addEnds(list);
console.log(sum);
